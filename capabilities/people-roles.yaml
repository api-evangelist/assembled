naftiko: 1.0.0-alpha2
info:
  label: Assembled People API — Roles
  description: 'Assembled People API — Roles. 1 operation. Lead operation: Assembled List Roles.'
  tags:
    - Assembled
    - Roles
  created: '2026-05-24'
  modified: '2026-05-24'
binds:
  - namespace: env
    keys:
      ASSEMBLED_API_KEY: ASSEMBLED_API_KEY
capability:
  consumes:
    - type: http
      namespace: people-roles
      baseUri: https://api.assembledhq.com
      description: Assembled People API — Roles business capability.
      resources:
        - name: v0-roles
          path: /v0/roles
          operations:
            - name: listroles
              method: GET
              description: Assembled List Roles
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: $.
      authentication:
        type: basic
        username: '{{env.ASSEMBLED_API_KEY}}'
        password: ''
  exposes:
    - type: mcp
      namespace: people-roles-mcp
      port: 9090
      transport: http
      description: MCP adapter for Assembled Roles.
      tools:
        - name: assembled-list-roles
          description: Assembled List Roles
          hints: { readOnly: true, destructive: false, idempotent: true }
          call: people-roles.listroles
          outputParameters:
            - type: object
              mapping: $.
